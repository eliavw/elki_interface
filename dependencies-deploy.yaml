name: elki_interface 
channels:
  - defaults
  - conda-forge
  - pytorch
dependencies:
  - python>=3.6
  - pip:
    - numpy
    - scipy
    - pandas
    - ipython
    - matplotlib
    - scikit-learn
    - prefect
